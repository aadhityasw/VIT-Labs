# Job Scheduling Algorithms (Lab 8)


### 1. Scheduling Algorithms

Demonstrate the working of Job Scheduling Algorithms like `First Come First Serve`(FCFS), `Shortest Job First`(SJF), Priority based, and Round Robin(RR).
