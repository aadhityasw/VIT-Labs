#include<stdio.h>
#define addi(a,b) (a+b)  
int main()
{
	int a,b,s;
	scanf("%d %d",&a,&b);
	s=addi(a,b);
	printf("%d",printf("%d %d %d\n",a,b,s));
	//It prints the answer and the number of characters printed.
}
